import styled from 'styled-components';

export default Dashboard;

type Props = {};

function Dashboard({}: Props) {
   return <Page>Você só pode entrar aqui com permissão</Page>;
}

const Page = styled.div``;
