import styled from 'styled-components';

export default styled.input.attrs({
   required: true,
})`
   width: 100%;
   padding: 0.8rem;

   border: none;
   border-radius: 0.4rem;

   font-size: 1.8rem;
`;
